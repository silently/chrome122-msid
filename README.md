# Missing msid in SDP (demo)

Try the following in Chrome 122 (versus older version or other browsers):

https://silently.github.io/chrome122-msid/

Or clone and launch locally with:

```sh
python3 -m http.server
```